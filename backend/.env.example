
# URL de connexion à MongoDB
# Format: mongodb+srv://username:password@cluster.mongodb.net/database
DATABASE_URL=mongodb+srv://votre-user:votre-password@cluster.mongodb.net/database

# Clé secrète JWT pour l'authentification
# Générer avec: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# DOIT ÊTRE au minimum 64 caractères aléatoires
JWT_SECRET=votre-secret-jwt-tres-long-minimum-64-caracteres-aleatoires

# Port d'écoute du serveur
PORT=3000

# Mode d'environnement (développement ou production)
NODE_ENV=development

# Configuration DeepL pour la traduction
# Clé API DeepL (obtenir sur https://www.deepl.com/pro-api)
DEEPL_API_KEY=votre-cle-api-deepl
# Utiliser l'API payante DeepL (true/false)
DEEPL_API_USE_PAID=false
# Timeout des requêtes DeepL (en millisecondes)
DEEPL_TIMEOUT_MS=15000

# Configuration Nodemailer pour l'envoi d'emails (optionnel - pour reset password)
# Service email utilisé (gmail, outlook, etc.)
EMAIL_SERVICE=gmail
# Adresse email d'envoi
EMAIL_USER=votre-email@gmail.com
# Mot de passe d'application Gmail (pas votre mot de passe normal)
# Générer sur: https://myaccount.google.com/apppasswords
EMAIL_PASSWORD=votre-mot-de-passe-application
# URL du client frontend pour les liens de réinitialisation
CLIENT_URL=http://localhost:5173
